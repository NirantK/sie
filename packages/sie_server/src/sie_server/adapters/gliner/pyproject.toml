[project]
name = "sie-adapter-gliner"
version = "0.1.0"
description = "GLiNER adapter for SIE - zero-shot named entity recognition"
requires-python = ">=3.12"
dependencies = [
    "gliner>=0.2,<1",
    "transformers>=4.51.3,<5",
    "torch>=2.9,<2.10",
]
