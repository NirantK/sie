[project]
name = "sie-adapter-sglang"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    # SGLang 0.5.6+ includes all deps at base level (fixed from 0.4.x extras bug)
    # See: https://github.com/sgl-project/sglang/issues/4869
    "sglang>=0.5.6",
]
